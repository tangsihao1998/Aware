//Define a Models
var Cart = require('../models/cart');
 
let controller ={};

controller.AddCart = async (cart) => {
    try {
        
    } catch (error) {
        console.log(error);
        return false;
    }
}


module.exports = controller;